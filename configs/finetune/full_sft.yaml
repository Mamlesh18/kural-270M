# Full fine-tuning (every weight). Needs ~4-5 GB RAM for 270M in fp32 and is ~2x slower
# than LoRA on CPU; prefer frozen_embeddings or LoRA on a laptop.
defaults:
  - lora_sft_cpu.yaml

run_name: full-sft-tamil
finetune:
  method: full
train:
  args:
    learning_rate: 3.0e-5
